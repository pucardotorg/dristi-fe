/**
 * The ground a document page sits on, wherever View Case previews one.
 *
 * It was `surface-sunken`, a warm near-white. A court PDF is mostly white, and
 * on the monitors many advocates work from the two bled into each other, so
 * the page lost its edge (owner, Sept 18). `secondary` is the ramp's next warm
 * step that reads as a fill rather than a tint: neutral-5 in light, neutral-4
 * in dark, where the page itself is the bright thing and needs less help.
 *
 * One constant on purpose. To try another tone, change it here and every
 * preview follows: the Case file, the order, application and document
 * records, and the digital record's thumbnails.
 */
export const DOCUMENT_GROUND = "bg-secondary";
